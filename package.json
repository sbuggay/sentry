{
  "name": "sentry",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "npm i && lerna bootstrap",
    "clean": "lerna clean --yes",
    "publish": "lerna run build && lerna publish --skip-git",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5",
    "ts-jest": "^20.0.7"
  }
}
