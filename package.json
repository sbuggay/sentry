{
  "name": "sentry",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "npm i && lerna bootstrap",
    "clean": "lerna clean --yes",
    "publish": "lerna publish --skip-git",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.38"
  }
}
